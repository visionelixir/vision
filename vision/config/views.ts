import { ViewConfig } from '@visionelixir/elixir'

const VIEW_CONFIG: ViewConfig = {
  serviceViewDirectory: 'views',
  themesDirectory: 'themes',
  themeFallback: ['mySite', 'base'],
}

export default VIEW_CONFIG
